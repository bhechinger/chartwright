apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "basic-chart.fullname" . | quote }}
  namespace: {{ .Release.Namespace | quote }}
data:
  chart: {{ .Chart.Name | quote }}
  name: {{ .Values.name | quote }}
  nested: |{{ toYaml .Values | nindent 4 }}
